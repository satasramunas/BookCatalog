@model BookCatalog.Dtos.BookDto

@using (Html.BeginForm("BorrowBook", "Book", FormMethod.Post))
{
    @Html.Label("Are you sure?")
        
    <input type="submit" value="Confirm" class="btn btn-primary"/>
}